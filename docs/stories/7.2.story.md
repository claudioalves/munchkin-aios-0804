# Story 7.2 — Modal ProgressChart (Recharts)

**Epic:** EP-07 — Gráfico de Evolução
**Story ID:** EP07-S02
**Status:** InReview
**Points:** 5
**Branch:** `story/7.2-progress-chart`

## Contexto

Modal com gráfico de linhas mostrando a evolução dos níveis ao longo da partida. Eixo X = tempo dos snapshots (formatado como HH:mm), eixo Y = nível (1 a max_level). Cada jogador é representado por uma linha com a sua cor de jogador.

## Acceptance Criteria

- [x] AC1: Componente existe em `apps/web/src/components/ProgressChart/ProgressChart.tsx`
- [x] AC2: Usa `LineChart` do `recharts` (já instalado)
- [x] AC3: Cada linha tem a cor do jogador respectivo (`player.color`)
- [x] AC4: Eixo Y vai de 1 até `maxLevel` (prop recebida)
- [x] AC5: Quando há menos de 2 snapshots, exibe estado vazio com mensagem `"Aguardando dados..."`
- [x] AC6: Modal fechável via clique no backdrop ou botão X
- [x] AC7: `npm run lint` e `npm run typecheck` passam sem erros

## Tasks de Implementação

- [x] T1: Criar `apps/web/src/components/ProgressChart/ProgressChart.tsx` com `LineChart` do Recharts
- [x] T2: Criar `apps/web/src/hooks/useSnapshots.ts` que busca e retorna snapshots via `getSnapshots`
- [x] T3: Integrar modal no `GamePage` com botão de abertura e estado `isChartOpen`
- [x] T4: Executar `npm run lint` e `npm run typecheck`

## Notas Técnicas

Dados agrupados por timestamp formatado como HH:mm. Chaves dinâmicas por nome de jogador.

## Dependências

Story 7.1 (getSnapshots). `recharts` já instalado em `apps/web`.

## File List

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| `apps/web/src/components/ProgressChart/ProgressChart.tsx` | CRIADO | Modal com LineChart Recharts, empty state, backdrop close |
| `apps/web/src/hooks/useSnapshots.ts` | CRIADO | Hook que busca snapshots via getSnapshots |
| `apps/web/src/pages/GamePage.tsx` | MODIFICADO | Estado isChartOpen + ProgressChart integrado |
