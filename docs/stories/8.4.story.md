# Story 8.4 — Reordenação por teclado

**Epic:** EP-08 — Ordenação & Drag-and-Drop
**Story ID:** EP08-S04
**Status:** InReview
**Points:** 2
**Branch:** `story/8.4-keyboard-reorder`

## Contexto

`@dnd-kit` suporta acessibilidade de teclado nativamente via `KeyboardSensor`. Esta story garante que a configuração de acessibilidade está correta no `DndContext` do `PlayerGrid`.

## Acceptance Criteria

- [x] AC1: `KeyboardSensor` adicionado ao array de `sensors` do `DndContext` no `PlayerGrid`
- [x] AC2: Teclas de seta movem os cards quando `sortMode === 'custom'` e um card está focado
- [x] AC3: Anúncio acessível de reordenação configurado via `accessibility.announcements` no `DndContext` (aria-live region)
- [x] AC4: `npm run lint` e `npm run typecheck` passam sem erros

## Tasks de Implementação

- [x] T1: Adicionar `KeyboardSensor` ao `useSensors` no `DndContext` do `PlayerGrid`
- [x] T2: Configurar `accessibility.announcements` no `DndContext` com mensagens em português
- [x] T3: Executar `npm run lint` e `npm run typecheck`

## Notas Técnicas

`sortableKeyboardCoordinates` de `@dnd-kit/sortable` usado como `coordinateGetter`.

## Dependências

Story 8.2 (Drag & Drop com DndContext já configurado).

## File List

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| `apps/web/src/components/PlayerGrid/PlayerGrid.tsx` | MODIFICADO | KeyboardSensor + sortableKeyboardCoordinates + accessibility.announcements |
