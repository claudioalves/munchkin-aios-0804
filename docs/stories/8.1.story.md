# Story 8.1 — SortDropdown

**Epic:** EP-08 — Ordenação & Drag-and-Drop
**Story ID:** EP08-S01
**Status:** InReview
**Points:** 2
**Branch:** `story/8.1-sort-dropdown`

## Contexto

Dropdown ou toggle que permite ao usuário escolher entre 3 modos de ordenação dos cards de jogadores: "Por Nível" (`level-desc`), "Aleatório" (`random`) e "Personalizado" (`custom`). A seleção atualiza o `sortMode` no Zustand store.

## Acceptance Criteria

- [x] AC1: Componente existe em `apps/web/src/components/SortDropdown/SortDropdown.tsx`
- [x] AC2: Exibe as 3 opções com ícones ou labels claros: "Por Nível", "Aleatório", "Personalizado"
- [x] AC3: Modo atualmente selecionado é destacado visualmente (cor dourada, negrito)
- [x] AC4: Ao selecionar uma opção, chama `setSortMode()` do Zustand store com o valor correspondente
- [x] AC5: Componente integrado no `GamePage`, acima do `PlayerGrid`
- [x] AC6: `npm run lint` e `npm run typecheck` passam sem erros

## Tasks de Implementação

- [x] T1: Criar `apps/web/src/components/SortDropdown/SortDropdown.tsx`
- [x] T2: Integrar `SortDropdown` no `GamePage` acima do `PlayerGrid`
- [x] T3: Executar `npm run lint` e `npm run typecheck`

## Notas Técnicas

Implementado como grupo de 3 botões com destaque dourado para o modo ativo.

## Dependências

Store Zustand com `sortMode` e `setSortMode` (EP-05, Story 5.1). Base para Stories 8.2 e 8.3.

## File List

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| `apps/web/src/components/SortDropdown/SortDropdown.tsx` | CRIADO | Grupo de botões com 3 modos de ordenação |
| `apps/web/src/pages/GamePage.tsx` | MODIFICADO | SortDropdown integrado acima do PlayerGrid |
