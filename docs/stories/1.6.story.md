# Story 1.6 — CI/CD pipeline

**Epic:** EP-01 — Setup & Infraestrutura
**Story ID:** EP01-S06
**Status:** Ready for Review
**Points:** 2
**Branch:** `story/1.6-cicd-pipeline`

## Contexto

Nenhuma feature deve chegar à `main` sem passar por lint, typecheck e testes automatizados. Esta story configura o pipeline de CI com GitHub Actions que executa as verificações de qualidade em todo push e pull request, e o pipeline de CD que faz deploy automático na Vercel quando um merge ocorre na branch `main`.

O CI protege o repositório contra regressões e garante que os padrões definidos no coding-standards (TypeScript strict, ESLint sem erros, testes passando) sejam enforçados de forma automática — não dependendo da disciplina manual de cada desenvolvedor. O CD elimina a fricção do deploy manual, permitindo que qualquer merge na main resulte em uma versão publicada em minutos.

## Acceptance Criteria

- [x] AC1: `.github/workflows/ci.yml` existe e é acionado em `push` e `pull_request` para qualquer branch
- [x] AC2: O job CI executa em sequência: `npm install` → `npm run lint` → `npm run typecheck` → `npm test`
- [ ] AC3: Um push com erro de lint ou typecheck falha o CI e bloqueia o merge do PR (status check obrigatório) — verificar após push para GitHub
- [x] AC4: `vercel.json` existe na raiz com configuração de deploy apontando para `apps/web` como root
- [ ] AC5: O projeto está conectado ao Vercel (via `vercel link` ou importação no dashboard) com deploy automático configurado para a branch `main` — **ação manual**
- [ ] AC6: Um merge na branch `main` aciona o deploy Vercel e a URL de preview fica disponível no PR como comentário — **verificação manual**
- [ ] AC7: As variáveis de ambiente (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`) estão configuradas no Vercel Dashboard — **ação manual**
- [x] AC8: O CI usa cache de `node_modules` para acelerar execuções repetidas (actions/cache para npm)

## Tasks de Implementação

- [x] T1: Criar diretório `.github/workflows/` na raiz do repositório
- [x] T2: Criar `.github/workflows/ci.yml` com job `quality-check`
- [ ] T3: Verificar que o repositório está no GitHub e o GitHub Actions está habilitado — **verificação manual**
- [ ] T4: Fazer push de uma branch de teste para acionar o CI — **verificação manual**
- [x] T5: Criar `vercel.json` na raiz com configuração do monorepo
- [ ] T6: Executar `vercel login` e `vercel link` na raiz do projeto — **ação manual**
- [ ] T7: Configurar no Vercel Dashboard as variáveis de ambiente de produção — **ação manual**
- [ ] T8: Configurar no Vercel Dashboard que deploys automáticos ocorrem apenas para `main` — **ação manual**
- [ ] T9: Fazer um merge de teste para `main` e verificar deploy — **verificação manual**
- [ ] T10: Verificar que a URL de deploy Vercel exibe o app corretamente — **verificação manual**
- [ ] T11: (Opcional) Adicionar Supabase keys como GitHub Actions Secrets — **ação manual**

## Notas Técnicas

**`.github/workflows/ci.yml`:**
```yaml
name: CI
on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']
jobs:
  quality-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
```

**`vercel.json` (monorepo com apps/web como root):**
```json
{
  "buildCommand": "npm run build --workspace=apps/web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm ci",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
```

**Por que `npm ci` e não `npm install` no CI:**
`npm ci` é determinístico — instala exatamente as versões do `package-lock.json`. Mais rápido e seguro para CI.

**Cache de node_modules:** O `cache: 'npm'` no `actions/setup-node@v4` usa o `package-lock.json` como chave de cache. Reduz o tempo de CI de ~2min para ~30s.

**Rewrite para SPA:** O `rewrites` no `vercel.json` garante que rotas do React Router funcionem em produção sem retornar 404.

**Referências:** `docs/framework/tech-stack.md` (GitHub Actions, Vercel)

## Dependências

**Story 1.1** deve estar concluída (monorepo com scripts `lint`, `typecheck`, `test` funcionando).

## File List

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| `.github/workflows/ci.yml` | Created | GitHub Actions workflow: lint + typecheck + tests com cache npm |
| `vercel.json` | Created | Config de deploy Vercel para monorepo: apps/web como root, SPA rewrites |
