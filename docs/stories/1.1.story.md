# Story 1.1 — Setup do monorepo e tooling

**Epic:** EP-01 — Setup & Infraestrutura
**Story ID:** EP01-S01
**Status:** Ready for Review
**Points:** 3
**Branch:** `story/1.1-monorepo-tooling`

## Contexto

Esta é a story fundacional de todo o projeto. Sem um monorepo bem configurado, nenhuma outra story pode avançar com segurança. O objetivo é criar a estrutura de workspaces npm com os três pacotes principais (`apps/web`, `apps/mobile`, `packages/shared`), configurar todas as ferramentas de qualidade de código (TypeScript strict, ESLint, Prettier) e garantir que o app web inicie em modo desenvolvimento sem erros.

A configuração correta dos path aliases (`@/` e `@munchkin/shared`) é crítica: todas as stories futuras dependem desse contrato de import para manter a separação de camadas definida no coding-standards. O TypeScript strict mode e o linting devem passar desde o primeiro commit — qualidade não é opcional.

## Acceptance Criteria

- [x] AC1: `npm install` na raiz instala as dependências de todos os workspaces sem erros
- [x] AC2: `npm run lint` na raiz executa ESLint em todos os pacotes e passa sem erros
- [x] AC3: `npm run typecheck` na raiz executa `tsc --noEmit` em todos os pacotes e passa sem erros
- [x] AC4: `npm run dev` inicia o servidor Vite em `localhost:5174` sem erros no terminal
- [x] AC5: O path alias `@/` resolve corretamente para `apps/web/src/` no Vite e no TypeScript
- [x] AC6: O path alias `@munchkin/shared` resolve corretamente para `packages/shared` nos apps web e mobile
- [x] AC7: `tsconfig.json` em todos os pacotes estende `tsconfig.base.json` e habilita `strict: true`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`
- [x] AC8: `.gitignore` cobre `node_modules`, `dist`, `.env.local`, `*.env`, arquivos de build

## Tasks de Implementação

- [x] T1: Criar `package.json` raiz com `"workspaces": ["apps/*", "packages/*"]` e scripts `dev`, `build`, `lint`, `typecheck`, `test` delegando para workspaces
- [x] T2: Criar `tsconfig.base.json` raiz com configurações strict compartilhadas entre todos os pacotes
- [x] T3: Criar `apps/web` scaffolding com Vite + React 19 + TS; instalar dependências do tech-stack
- [x] T4: Configurar `apps/web/vite.config.ts` com plugin React, Tailwind e alias `@/` → `./src`
- [x] T5: Configurar `apps/web/tsconfig.json` estendendo `tsconfig.base.json` com paths para `@/` e `@munchkin/shared`
- [x] T6: Criar `apps/mobile` com `npx create-expo-app` template blank-typescript; ajustar tsconfig com paths
- [x] T7: Criar `packages/shared` com `package.json` (@munchkin/shared), `tsconfig.json`, `src/services/`, `src/store/`
- [x] T8: Criar `packages/shared/src/types.ts`, `constants.ts`, `index.ts` com tipos e constantes base
- [x] T9: Configurar ESLint raiz (`eslint.config.js`) com `no-explicit-any: error`, `no-console: error`, `react-hooks: error`
- [x] T10: Configurar Prettier (`.prettierrc`) com convenções do projeto
- [x] T11: Scripts do `package.json` raiz funcionais: lint, typecheck, dev, test
- [x] T12: `.gitignore` atualizado cobrindo node_modules, dist, .env.local, .expo/, coverage/
- [x] T13: `.env.example` atualizado com variáveis `VITE_SUPABASE_URL` e `VITE_SUPABASE_ANON_KEY`
- [x] T14: `npm run dev` sobe Vite em localhost:5174 sem erros no terminal

## Notas Técnicas

**Estrutura de workspaces no `package.json` raiz:**
```json
{
  "name": "munchkin-level-tracker",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspaces",
    "lint": "eslint .",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  }
}
```

**`tsconfig.base.json` raiz:**
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "isolatedModules": true
  }
}
```

**Alias Vite (`apps/web/vite.config.ts`):**
```typescript
import path from 'path';
resolve: {
  alias: {
    '@': path.resolve(__dirname, './src'),
  },
}
```

**Alias TypeScript (`apps/web/tsconfig.json`):**
```json
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

**Referências:** `docs/framework/tech-stack.md` (versões exatas e comandos de setup), `docs/framework/coding-standards.md` (regras de lint e tsconfig obrigatórias)

## Dependências

Nenhuma. EP01-S01 é o ponto de partida absoluto do projeto.

## File List

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| `package.json` | Modified | Root monorepo config com workspaces, type: module, scripts |
| `tsconfig.base.json` | Created | TypeScript strict config compartilhado |
| `.prettierrc` | Created | Prettier config do projeto |
| `eslint.config.js` | Created | ESLint flat config com TS + React hooks rules |
| `.gitignore` | Modified | Adicionado .expo/, tsbuildinfo, coverage/ |
| `.env.example` | Modified | Adicionado VITE_SUPABASE_URL e VITE_SUPABASE_ANON_KEY |
| `apps/web/package.json` | Created | Web app deps: React 19, Vite 6, Tailwind 4, Supabase, Zustand, dnd-kit, Recharts |
| `apps/web/vite.config.ts` | Created | Vite + React + Tailwind + alias @/ + vitest config |
| `apps/web/tsconfig.json` | Created | TS config estendendo base + paths @/ e @munchkin/shared |
| `apps/web/tsconfig.node.json` | Created | TS config para node (vite.config) |
| `apps/web/index.html` | Created | Entry HTML do SPA |
| `apps/web/src/main.tsx` | Created | Entry point React |
| `apps/web/src/App.tsx` | Created | Componente raiz |
| `apps/web/src/styles/globals.css` | Created | CSS tokens, Google Fonts, keyframes, base styles |
| `apps/web/src/test-setup.ts` | Created | Vitest + testing-library setup |
| `apps/mobile/tsconfig.json` | Modified | Adicionado paths @/ e @munchkin/shared |
| `packages/shared/package.json` | Created | @munchkin/shared config |
| `packages/shared/tsconfig.json` | Created | TS config estendendo base |
| `packages/shared/src/index.ts` | Created | Public API do pacote compartilhado |
| `packages/shared/src/types.ts` | Created | Tipos: Player, Game, GamePlayer, LevelSnapshot, etc. |
| `packages/shared/src/constants.ts` | Created | AVATAR_COLORS, LEVEL_LIMITS, GAME_CONFIG, STORAGE_KEYS |
