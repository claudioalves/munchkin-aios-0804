# Story 1.1 — Setup do monorepo e tooling

**Epic:** EP-01 — Setup & Infraestrutura
**Story ID:** EP01-S01
**Status:** Draft
**Points:** 3
**Branch:** `story/1.1-monorepo-tooling`

## Contexto

Esta é a story fundacional de todo o projeto. Sem um monorepo bem configurado, nenhuma outra story pode avançar com segurança. O objetivo é criar a estrutura de workspaces npm com os três pacotes principais (`apps/web`, `apps/mobile`, `packages/shared`), configurar todas as ferramentas de qualidade de código (TypeScript strict, ESLint, Prettier) e garantir que o app web inicie em modo desenvolvimento sem erros.

A configuração correta dos path aliases (`@/` e `@munchkin/shared`) é crítica: todas as stories futuras dependem desse contrato de import para manter a separação de camadas definida no coding-standards. O TypeScript strict mode e o linting devem passar desde o primeiro commit — qualidade não é opcional.

## Acceptance Criteria

- [ ] AC1: `npm install` na raiz instala as dependências de todos os workspaces sem erros
- [ ] AC2: `npm run lint` na raiz executa ESLint em todos os pacotes e passa sem erros
- [ ] AC3: `npm run typecheck` na raiz executa `tsc --noEmit` em todos os pacotes e passa sem erros
- [ ] AC4: `npm run dev` (ou `npm run dev --workspace=apps/web`) inicia o servidor Vite em `localhost:5173` sem erros no terminal
- [ ] AC5: O path alias `@/` resolve corretamente para `apps/web/src/` no Vite e no TypeScript
- [ ] AC6: O path alias `@munchkin/shared` resolve corretamente para `packages/shared` nos apps web e mobile
- [ ] AC7: `tsconfig.json` em todos os pacotes estende `tsconfig.base.json` e habilita `strict: true`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`
- [ ] AC8: `.gitignore` cobre `node_modules`, `dist`, `.env.local`, `*.env`, arquivos de build

## Tasks de Implementação

- [ ] T1: Criar `package.json` raiz com `"workspaces": ["apps/*", "packages/*"]` e scripts `dev`, `build`, `lint`, `typecheck`, `test` delegando para workspaces
- [ ] T2: Criar `tsconfig.base.json` raiz com configurações strict compartilhadas entre todos os pacotes
- [ ] T3: Criar `apps/web` com `npm create vite@latest` usando template `react-ts`; instalar todas as dependências listadas no tech-stack (React 19, Zustand 5, React Router 7, Tailwind 4, Supabase JS, dnd-kit, recharts, phosphor-icons, sonner, vitest, testing-library)
- [ ] T4: Configurar `apps/web/vite.config.ts` com plugin React e alias `@/` → `./src`
- [ ] T5: Configurar `apps/web/tsconfig.json` estendendo `tsconfig.base.json` e incluindo paths para `@/`
- [ ] T6: Criar `apps/mobile` com `npx create-expo-app` usando template `blank-typescript`; instalar dependências mobile (Expo Router 4, NativeWind 4, Reanimated 3, draggable-flatlist, victory-native)
- [ ] T7: Criar `packages/shared` manualmente: `package.json` com `"name": "@munchkin/shared"`, `tsconfig.json`, estrutura de diretórios `src/services/`, `src/store/`
- [ ] T8: Criar arquivos placeholder em `packages/shared/src/` (`types.ts`, `constants.ts`, `index.ts`) para que o pacote seja importável
- [ ] T9: Configurar ESLint raiz (`eslint.config.js`) com regras do coding-standards: `@typescript-eslint/no-explicit-any: error`, `no-console: error (allow error/warn)`, `react-hooks/rules-of-hooks: error`
- [ ] T10: Configurar Prettier (`.prettierrc`) com as convenções do projeto
- [ ] T11: Adicionar scripts no `package.json` raiz: `"lint": "eslint apps/*/src packages/*/src"`, `"typecheck": "tsc --noEmit -p tsconfig.base.json && npm run typecheck --workspaces"`
- [ ] T12: Criar `.gitignore` cobrindo `node_modules`, `dist`, `.env.local`, `.env`, `*.log`, `.DS_Store`
- [ ] T13: Criar `.env.example` com as variáveis documentadas (sem valores): `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`
- [ ] T14: Verificar que `npm run dev` sobe o Vite web app e exibe a página padrão React sem erros de console

## Notas Técnicas

**Estrutura de workspaces no `package.json` raiz:**
```json
{
  "name": "munchkin-level-tracker",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspaces",
    "lint": "eslint .",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  }
}
```

**`tsconfig.base.json` raiz:**
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "isolatedModules": true
  }
}
```

**Alias Vite (`apps/web/vite.config.ts`):**
```typescript
import path from 'path';
resolve: {
  alias: {
    '@': path.resolve(__dirname, './src'),
  },
}
```

**Alias TypeScript (`apps/web/tsconfig.json`):**
```json
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

**Referências:** `docs/framework/tech-stack.md` (versões exatas e comandos de setup), `docs/framework/coding-standards.md` (regras de lint e tsconfig obrigatórias)

## Dependências

Nenhuma. EP01-S01 é o ponto de partida absoluto do projeto.

## File List (atualizar durante implementação)

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| *(preencher durante implementação)* | | |
