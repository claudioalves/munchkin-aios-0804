# Story 2.1 — Tela de Gestão de Jogadores

**Epic:** EP-02 — Gerenciamento de Jogadores
**Story ID:** EP02-S01
**Status:** Ready for Review
**Points:** 3
**Branch:** `story/2.1-players-screen`

## Contexto

Para que um usuário possa cadastrar jogadores e iniciar partidas, o app precisa de uma tela dedicada que liste todos os jogadores registrados para aquela sessão anônima. Esta story implementa:

1. O **roteador React Router** (instalado nesta story, pois é necessário para navegar entre telas)
2. A **tela de gestão de jogadores** (`/players`) com lista paginada por scroll
3. O **serviço `getPlayers()`** em `packages/shared/src/services/players.ts`
4. A **tela inicial** (home / menu) com navegação básica

O App.tsx atual é um smoke test do design system — esta story o substitui pela estrutura real de rotas.

## Acceptance Criteria

- [x] AC1: `react-router-dom` está instalado em `apps/web` e configurado com `createBrowserRouter`
- [x] AC2: A rota `/` exibe a tela Home com botões "Gerenciar Jogadores" e "Novo Jogo" (Novo Jogo pode ser placeholder por enquanto)
- [x] AC3: A rota `/players` exibe a tela de gestão de jogadores
- [x] AC4: A tela `/players` busca jogadores do Supabase via `getPlayers()` e os exibe em lista
- [x] AC5: Cada item da lista mostra: avatar com inicial + cor, nome do jogador
- [x] AC6: Estado vazio exibe mensagem "Nenhum jogador cadastrado ainda" com CTA para adicionar
- [x] AC7: Indicador de loading durante a busca dos dados
- [x] AC8: Erros de rede exibem mensagem de erro com opção de retry
- [x] AC9: `getPlayers()` em `packages/shared/src/services/players.ts` exportado via `index.ts`
- [x] AC10: `npm run lint` e `npm run typecheck` passam sem erros

## Tasks de Implementação

- [x] T1: Instalar react-router-dom: `npm install react-router-dom --workspace=apps/web`
- [x] T2: Criar `packages/shared/src/services/players.ts` com `getPlayers(supabase)` usando injeção de dependência
- [x] T3: Exportar `getPlayers` via `packages/shared/src/index.ts`
- [x] T4: Criar `apps/web/src/pages/Home.tsx` — tela inicial com botões de navegação
- [x] T5: Criar `apps/web/src/pages/PlayersPage.tsx` — tela de gestão (lista + empty state + loading + error)
- [x] T6: Criar `apps/web/src/components/PlayerListItem/PlayerListItem.tsx` — item da lista
- [x] T7: Criar `apps/web/src/router.tsx` com `createBrowserRouter` (rotas: `/` e `/players`)
- [x] T8: Atualizar `apps/web/src/App.tsx` para usar `RouterProvider`
- [x] T9: Executar `npm run lint` e `npm run typecheck`

## Notas Técnicas

**`packages/shared/src/services/players.ts`:**
```typescript
import type { SupabaseClient } from '@supabase/supabase-js';
import type { Database } from '../database.types';
import type { Player } from '../types';

export async function getPlayers(supabase: SupabaseClient<Database>): Promise<Player[]> {
  const { data, error } = await supabase
    .from('players')
    .select('*')
    .order('created_at', { ascending: true });
  if (error) throw new Error(`Failed to fetch players: ${error.message}`);
  return data ?? [];
}
```

**`apps/web/src/router.tsx`:**
```typescript
import { createBrowserRouter } from 'react-router-dom';
import Home from '@/pages/Home';
import PlayersPage from '@/pages/PlayersPage';

export const router = createBrowserRouter([
  { path: '/', element: <Home /> },
  { path: '/players', element: <PlayersPage /> },
]);
```

**`apps/web/src/App.tsx` (novo):**
```typescript
import { RouterProvider } from 'react-router-dom';
import { router } from './router';

export default function App() {
  return <RouterProvider router={router} />;
}
```

**`apps/web/src/pages/PlayersPage.tsx` — estrutura:**
```typescript
import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { supabase } from '@/lib/supabase';
import { getPlayers } from '@munchkin/shared';
import type { Player } from '@munchkin/shared';

export default function PlayersPage() {
  const [players, setPlayers] = useState<Player[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const load = async () => {
    setLoading(true);
    setError(null);
    try {
      setPlayers(await getPlayers(supabase));
    } catch (e) {
      setError(e instanceof Error ? e.message : 'Erro ao carregar jogadores');
    } finally {
      setLoading(false);
    }
  };

  useEffect(() => { void load(); }, []);

  // render: loading spinner | error state | empty state | list
}
```

**`PlayerListItem` — exibe avatar circular com cor + nome:**
```typescript
interface PlayerListItemProps {
  player: { id: string; name: string; color: string };
}
```

**Estilo do avatar:** `w-10 h-10 rounded-full flex items-center justify-center font-heading text-surface-base text-sm font-bold` com `style={{ backgroundColor: player.color }}`

## Dependências

**Story 1.1** (monorepo), **Story 1.2** (Supabase), **Story 1.3** (schema/RLS), **Story 1.4** (auth), **Story 1.5** (design system) devem estar concluídas.

## File List (atualizar durante implementação)

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| `packages/shared/src/services/players.ts` | Created | `getPlayers()`, `createPlayer()`, `deletePlayer()` com injeção de dependência |
| `packages/shared/src/index.ts` | Modified | Exporta as 3 funções de players |
| `apps/web/src/pages/Home.tsx` | Created | Tela inicial com navegação para `/players` e `/new-game` |
| `apps/web/src/pages/PlayersPage.tsx` | Created | Lista de jogadores com loading, empty state e error state |
| `apps/web/src/components/PlayerListItem/PlayerListItem.tsx` | Created | Item com avatar circular colorido + nome |
| `apps/web/src/router.tsx` | Created | `createBrowserRouter` com rotas `/` e `/players` |
| `apps/web/src/App.tsx` | Modified | Substituído smoke test por `RouterProvider` |
