# Story 9.3 — Timer de jogo + botões Regras e NotebookLM

**Epic:** EP-09 — Melhorias de Experiência de Jogo
**Story ID:** EP09-S03
**Status:** InProgress
**Points:** 3
**Branch:** `story/9.3-game-timer`

## Contexto

Após o grid de jogadores, exibir: (a) hora de início da partida, (b) cronômetro em tempo real. `activeGame.started_at` já existe no banco. Também adicionar dois botões de acesso rápido: "Regras" (abre `/rules`) e "NotebookLM" (abre URL externa em nova aba).

## Acceptance Criteria

- [x] AC1: Componente `GameTimer` exibe "Início: HH:MM" formatado conforme timezone local
- [x] AC2: Cronômetro atualiza a cada segundo: "Xh Ym" ou "Ym Zs" (menos de 1h)
- [x] AC3: Timer aparece abaixo do `PlayerGrid` e acima dos botões de ação
- [x] AC4: Botão "📖 Regras" abre `/rules` em nova aba
- [x] AC5: Botão "🎙 NotebookLM" abre URL do NotebookLM em nova aba (URL configurável em constante)
- [x] AC6: Layout responsivo: timer + botões na mesma linha ou empilhados em telas pequenas
- [x] AC7: `npm run lint` e `npm run typecheck` passam

## Tasks de Implementação

- [x] T1: Criar `apps/web/src/components/GameTimer/GameTimer.tsx`
- [x] T2: Integrar `GameTimer` na `GamePage.tsx` após o `PlayerGrid`
- [x] T3: Adicionar botões Regras e NotebookLM na `GamePage.tsx`
- [x] T4: Atualizar traduções

## Notas Técnicas

- `activeGame.started_at` é ISO 8601 (UTC). Usar `new Date(started_at)` para converter
- `NOTEBOOKLM_URL` como constante editável no topo do arquivo ou em `apps/web/src/lib/config.ts`
- Cleanup do `setInterval` no return do `useEffect`

## Dependências

Story 9.2 (botão de regras referencia rota /rules).

## File List

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| `apps/web/src/components/GameTimer/GameTimer.tsx` | CRIADO | Timer de início e cronômetro |
| `apps/web/src/pages/GamePage.tsx` | MODIFICADO | Timer + botões Regras/NotebookLM integrados |
| `apps/web/src/i18n/translations.ts` | MODIFICADO | Chaves do timer |
