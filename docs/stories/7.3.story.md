# Story 7.3 — Botão de gráfico na barra

**Epic:** EP-07 — Gráfico de Evolução
**Story ID:** EP07-S03
**Status:** InReview
**Points:** 1
**Branch:** `story/7.3-chart-button`

## Contexto

Adicionar um botão de acesso rápido ao gráfico de evolução, visível no `AppHeader` ou abaixo do grid no `GamePage`. Ao clicar, abre o modal `ProgressChart`.

## Acceptance Criteria

- [x] AC1: Botão visível na `GamePage` (no `AppHeader`)
- [x] AC2: Ao clicar, abre o modal `ProgressChart`
- [x] AC3: `npm run lint` e `npm run typecheck` passam sem erros

## Tasks de Implementação

- [x] T1: Adicionar prop `onChartOpen` no `AppHeader`; controlar abertura do modal via estado `isChartOpen` no `GamePage`
- [x] T2: Executar `npm run lint` e `npm run typecheck`

## Notas Técnicas

Botão 📊 adicionado no canto direito do AppHeader substituindo o `<div className="w-14" />` placeholder. Fica invisível (opacity-0) quando `onChartOpen` não é fornecido para manter o layout centrado.

## Dependências

Story 7.2 (ProgressChart modal).

## File List

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| `apps/web/src/components/AppHeader/AppHeader.tsx` | MODIFICADO | Prop onChartOpen opcional; botão 📊 no canto direito |
