# Story 1.2 — Setup Supabase project

**Epic:** EP-01 — Setup & Infraestrutura
**Story ID:** EP01-S02
**Status:** Ready for Review
**Points:** 2
**Branch:** `story/1.2-supabase-setup`

## Contexto

Antes de qualquer migration ou funcionalidade de auth poder ser implementada, o projeto Supabase precisa existir e estar conectado ao ambiente de desenvolvimento local. Esta story cobre a criação manual do projeto no Supabase Dashboard, a configuração das variáveis de ambiente no app web e a geração dos tipos TypeScript do banco — o contrato tipado entre o frontend e o banco de dados.

Os tipos gerados em `packages/shared/src/database.types.ts` são o alicerce da type safety end-to-end do projeto. Toda chamada ao Supabase client (`supabase.from('players')`) usará esses tipos para garantir que o compilador capture erros de schema antes do runtime.

## Acceptance Criteria

- [x] AC1: Projeto Supabase criado no dashboard
- [x] AC2: `apps/web/.env.local` preenchido com credenciais reais
- [x] AC3: `apps/web/.env.local` está no `.gitignore` e NÃO está commitado
- [x] AC4: `supabase/config.toml` configurado (gerado via `supabase init`)
- [x] AC5: `supabase link --project-ref kohoibhscveqqvdtqphv` executado com sucesso
- [x] AC6: `packages/shared/src/database.types.ts` gerado via `supabase gen types typescript --linked`
- [x] AC7: `apps/web/src/lib/supabase.ts` exporta cliente tipado com `Database`
- [x] AC8: `npm run typecheck` passa ✅

## Tasks de Implementação

- [ ] T1: Acessar `supabase.com/dashboard`, criar novo projeto, anotar `Project URL` e `anon key` — **ação manual**
- [ ] T2: Criar `apps/web/.env.local` com credenciais reais — **ação manual**
- [x] T3: Supabase CLI disponível via `npx supabase` (v2.88.1)
- [x] T4: `supabase init` executado → `supabase/config.toml` gerado
- [ ] T5: `supabase link --project-ref <ref>` — **ação manual após T1**
- [x] T6: `supabase/migrations/` criado
- [x] T7: `packages/shared/src/database.types.ts` criado (placeholder; regenerar com `npm run db:types` após T5)
- [x] T8: `apps/web/src/lib/supabase.ts` criado com cliente tipado + `vite-env.d.ts`
- [x] T9: `npm run typecheck` passa ✅
- [x] T10: `.env.example` verificado (variáveis VITE_ documentadas desde Story 1.1)

## Notas Técnicas

**Localização do `project-ref`:** No Supabase Dashboard → Settings → General → Reference ID. Formato: string alfanumérica curta como `abcdefghijklmnop`.

**Comando de geração de tipos:**
```bash
supabase gen types typescript --linked > packages/shared/src/database.types.ts
```
O arquivo gerado deve ser re-executado sempre que o schema do banco mudar (após cada migration).

**Export no `packages/shared/src/index.ts`:**
```typescript
export type { Database } from './database.types';
```

**Atenção com tipos gerados:** O arquivo `database.types.ts` é auto-gerado — nunca editá-lo manualmente. Se o banco ainda estiver vazio (sem tabelas), o arquivo gerado terá um schema vazio `{}` — isso é esperado nesta story. As tabelas reais serão criadas na Story 1.3.

**Variáveis de ambiente no Vite:** Apenas variáveis prefixadas com `VITE_` são expostas ao browser. Nunca usar chaves secretas (service role key) — apenas a `anon key`.

**Referências:** `docs/framework/tech-stack.md` (seção 3 — Variáveis de Ambiente), Supabase CLI docs: `https://supabase.com/docs/reference/cli`

## Dependências

**Story 1.1** deve estar concluída (monorepo configurado, `packages/shared` com estrutura de diretórios criada).

## File List

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| `supabase/config.toml` | Created | Config Supabase CLI (gerado por `supabase init`) |
| `supabase/migrations/` | Created | Diretório de migrations (vazio, preenchido na 1.3) |
| `packages/shared/src/database.types.ts` | Created | Tipos gerados do DB (placeholder; regenerar após migration) |
| `packages/shared/src/index.ts` | Modified | Exporta `Database`, `Json`, `Tables`, etc. |
| `apps/web/src/lib/supabase.ts` | Created | Cliente Supabase tipado com `Database` |
| `apps/web/src/vite-env.d.ts` | Created | Tipos das variáveis de ambiente Vite |
| `package.json` | Modified | Adicionado script `db:types` |
