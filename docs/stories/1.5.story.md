# Story 1.5 — Design System base (Tailwind)

**Epic:** EP-01 — Setup & Infraestrutura
**Story ID:** EP01-S05
**Status:** Draft
**Points:** 2
**Branch:** `story/1.5-design-system`

## Contexto

O Munchkin Level Tracker tem uma identidade visual temática: tipografia medieval com Cinzel Decorative, paleta de cores de pergaminho e ouro (tons âmbar/dourados como `#c9943a`) e efeitos sutis de sombra que evocam o universo do jogo de cartas. Esta story configura o Tailwind CSS 4 com os tokens visuais definidos no Front-End Spec e cria a base CSS que todos os componentes das stories futuras irão consumir.

Uma página de teste (ou a página raiz do app) deve demonstrar visualmente que os tokens estão aplicados corretamente — cores, tipografia, espaçamentos. Isso serve como documentação viva do design system e como smoke test para garantir que `npm run dev` reflete fielmente os tokens configurados.

## Acceptance Criteria

- [ ] AC1: `tailwind.config.ts` em `apps/web` contém as cores customizadas do projeto (dourado, pergaminho, sombra) como tokens nomeados (ex: `brand.gold`, `brand.parchment`)
- [ ] AC2: As três fontes do projeto estão carregadas via Google Fonts no `index.html` ou via `@import` no CSS: `Cinzel Decorative`, `Cinzel`, `Crimson Pro`
- [ ] AC3: `apps/web/src/styles/globals.css` define CSS custom properties (`--color-gold`, `--font-display`, etc.) alinhadas com os tokens do Tailwind
- [ ] AC4: `npm run dev` exibe a página com tipografia correta (fonte display medieval visível) sem erros de console
- [ ] AC5: Classes Tailwind utilitárias de cor (`text-brand-gold`, `bg-brand-parchment`) e tipografia (`font-display`, `font-body`) funcionam em componentes
- [ ] AC6: O Tailwind `@tailwindcss/vite` plugin está configurado no `vite.config.ts` (Tailwind 4 não usa PostCSS por padrão)
- [ ] AC7: `npm run lint` e `npm run typecheck` passam sem erros

## Tasks de Implementação

- [ ] T1: Instalar Tailwind CSS 4 e o plugin Vite: `npm install tailwindcss@^4 @tailwindcss/vite --workspace=apps/web`
- [ ] T2: Adicionar plugin `@tailwindcss/vite` ao `apps/web/vite.config.ts`
- [ ] T3: Criar `apps/web/tailwind.config.ts` com os tokens de cor, tipografia e animação do projeto (detalhes abaixo)
- [ ] T4: Adicionar `@import "tailwindcss"` e as CSS custom properties no `apps/web/src/styles/globals.css`
- [ ] T5: Importar `globals.css` no `apps/web/src/main.tsx` (antes do render do React)
- [ ] T6: Adicionar tags `<link>` no `apps/web/index.html` para Google Fonts (Cinzel Decorative:400, Cinzel:400;600;700, Crimson Pro:400;600)
- [ ] T7: Criar uma página de teste visual em `apps/web/src/app/page.tsx` (ou atualizar a existente) exibindo: swatches de cor com nomes, amostras de tipografia nas 3 fontes, e um botão estilizado com tema dourado
- [ ] T8: Verificar no browser que as fontes carregam corretamente (DevTools → Network → Fonts)
- [ ] T9: Verificar que classes Tailwind customizadas como `text-brand-gold` aplicam a cor correta no elemento inspecionado
- [ ] T10: Executar `npm run lint` e `npm run typecheck` — corrigir qualquer erro

## Notas Técnicas

**`apps/web/tailwind.config.ts` — tokens principais:**
```typescript
import type { Config } from 'tailwindcss';

export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        brand: {
          gold:       '#c9943a',
          'gold-light': '#e8b86d',
          'gold-dark':  '#a0721e',
          parchment:  '#f5e6c8',
          'parchment-dark': '#e8d4a8',
          shadow:     '#1a0f00',
        },
        surface: {
          dark:   '#1c1208',
          medium: '#2d1f0a',
          light:  '#3d2b0f',
        },
      },
      fontFamily: {
        display: ['"Cinzel Decorative"', 'serif'],  // títulos principais
        heading: ['Cinzel', 'serif'],               // headings e labels
        body:    ['"Crimson Pro"', 'serif'],         // corpo de texto
      },
      boxShadow: {
        'card':    '0 4px 16px rgba(201, 148, 58, 0.15)',
        'card-hover': '0 8px 24px rgba(201, 148, 58, 0.30)',
        'glow-gold':  '0 0 12px rgba(201, 148, 58, 0.5)',
      },
      keyframes: {
        'level-bump': {
          '0%, 100%': { transform: 'scale(1)' },
          '50%':      { transform: 'scale(1.15)' },
        },
      },
      animation: {
        'level-bump': 'level-bump 0.3s ease-in-out',
      },
    },
  },
} satisfies Config;
```

**`apps/web/src/styles/globals.css` — estrutura base:**
```css
@import "tailwindcss";

:root {
  --color-gold:       #c9943a;
  --color-parchment:  #f5e6c8;
  --color-shadow:     #1a0f00;
  --font-display:     "Cinzel Decorative", serif;
  --font-heading:     "Cinzel", serif;
  --font-body:        "Crimson Pro", serif;
}

body {
  background-color: theme('colors.surface.dark');
  color: theme('colors.brand.parchment');
  font-family: var(--font-body);
}
```

**Google Fonts no `index.html`:**
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@400&family=Cinzel:wght@400;600;700&family=Crimson+Pro:wght@400;600&display=swap" rel="stylesheet">
```

**Tailwind 4 com Vite:** A versão 4 do Tailwind usa plugin nativo do Vite (`@tailwindcss/vite`) em vez de PostCSS. A configuração é feita em `vite.config.ts`, não em `postcss.config.js`.

**Referências:** `docs/front-end-spec.md` (tokens visuais completos, paleta de cores, tipografia definida), `docs/framework/tech-stack.md` (Tailwind CSS 4)

## Dependências

**Story 1.1** deve estar concluída (monorepo configurado, Vite funcionando, `apps/web` com estrutura de diretórios).

## File List (atualizar durante implementação)

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| *(preencher durante implementação)* | | |
