# Story 3.4 — Criar Game no Supabase

**Epic:** EP-03 — Fluxo de Novo Jogo
**Story ID:** EP03-S04
**Status:** Ready for Review
**Points:** 3
**Branch:** `story/3.4-create-game`

## Contexto

Ao confirmar o Passo 2 do stepper, o app cria o jogo no Supabase: um registro em `games` e N registros em `game_players` (um por jogador, level=1, position=index). Após criação bem-sucedida, o estado do jogo é salvo no Zustand store e o app navega para `/game`.

Esta story também cria a rota `/game` como placeholder — a tela de partida completa vem no EP-04.

## Acceptance Criteria

- [x] AC1: `createGame(supabase, playerIds, epicMode)` em `packages/shared/src/services/games.ts` cria game + game_players em sequência
- [x] AC2: Após criação, `setActiveGame(game)` e `setGamePlayers(gamePlayers)` são chamados no Zustand store
- [x] AC3: App navega para `/game` após criação bem-sucedida
- [x] AC4: Rota `/game` existe no router com placeholder `GamePage`
- [x] AC5: `createGame` exportado via `packages/shared/src/index.ts`
- [x] AC6: Erro de criação é exibido sem navegar (botão volta a habilitado)
- [x] AC7: `npm run lint` e `npm run typecheck` passam sem erros

## Tasks de Implementação

- [x] T1: Adicionar `createGame(supabase, playerIds, epicMode)` em `packages/shared/src/services/games.ts`
- [x] T2: Exportar `createGame` via `packages/shared/src/index.ts`
- [x] T3: Integrar `createGame` no `NewGameConfigPage` (Story 3.3) — salvar no Zustand e navegar
- [x] T4: Criar `apps/web/src/pages/GamePage.tsx` — placeholder "Tela de Partida (EP-04)"
- [x] T5: Adicionar rota `/game` ao `apps/web/src/router.tsx`
- [x] T6: Executar `npm run lint` e `npm run typecheck`

## Notas Técnicas

**`packages/shared/src/services/games.ts` — createGame:**
```typescript
export async function createGame(
  supabase: SupabaseClient<Database>,
  playerIds: string[],
  epicMode: boolean
): Promise<GameWithPlayers> {
  const { data: game, error: gameError } = await supabase
    .from('games')
    .insert({ epic_mode: epicMode, player_order: playerIds })
    .select()
    .single();
  if (gameError) throw new Error(`Failed to create game: ${gameError.message}`);
  if (!game) throw new Error('Game creation returned no data');

  const gpInserts = playerIds.map((playerId, index) => ({
    game_id: game.id,
    player_id: playerId,
    level: 1,
    position: index,
  }));

  const { data: gamePlayers, error: gpError } = await supabase
    .from('game_players')
    .insert(gpInserts)
    .select(`*, player:players (*)`);
  if (gpError) throw new Error(`Failed to create game players: ${gpError.message}`);

  return { ...game, game_players: gamePlayers ?? [] } as GameWithPlayers;
}
```

**Zustand após criação (em NewGameConfigPage):**
```typescript
const { setActiveGame, setGamePlayers } = useGameStore();
const result = await createGame(supabase, selectedIds, epicMode);
setActiveGame(result);
setGamePlayers(result.game_players);
navigate('/game');
```

## Dependências

Story 3.3 (step 2 UI).

## File List (atualizar durante implementação)

| Arquivo | Ação | Descrição |
|---------|------|-----------|
| `packages/shared/src/services/games.ts` | Modified (3.1) | createGame() adicionado |
| `packages/shared/src/index.ts` | Modified | createGame exportado |
| `apps/web/src/pages/GamePage.tsx` | Created | Placeholder com restauração do store via getActiveGame |
| `apps/web/src/router.tsx` | Modified | Rota /game adicionada |
